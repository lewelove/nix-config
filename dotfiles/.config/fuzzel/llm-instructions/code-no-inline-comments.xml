<instruction>
  - Write out the code.
  - For each file changed provide detailed description of logic changed.
</instruction>

<important_constraints>
  - Never write any commented lines within code blocks.
  - When implementing new logic never keep the backwards compatability logic.
  - If something useful to reach our goal can be done with `fish` cli commands, provide them first. Do not overuse them for direct file text editing, if a lot of changes required, print entire file instead.
</important_constraints>

  <output_formatting>
    {Here you explain changes made in the file}
  
    File: `path/to/filename.ext`
    ```
    {Full file content here}
    ```
  </output_formatting>
